[project]
name = "qwen3.5-reasoning-toggle-proxy"
version = "0.1.0"
description = "OpenAI-compatible proxy for Qwen3.5 models with thinking toggle"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.109.0",
    "httpx>=0.27.0",
    "uvicorn>=0.27.0",
]